/*
No Javascript, podemos criar variáveis de 3 formas:
var, let e const
*/
/*
// var -> variável global (não recomendado) -> não é mais utilizado 
var nome = "João"; // string (texto) -> aspas duplas ou simples 
var idade = 20; // number (número) -> sem aspas
var altura = 1.80; // number (número) -> sem aspas
var masculino = true; // boolean (verdadeiro ou falso) -> sem aspas
var masculino = false; // boolean (verdadeiro ou falso) -> sem aspas

// let -> variável local (recomendado) -> variável que só existe dentro do escopo onde foi criada 
let nome = "João"; // string (texto) -> aspas duplas ou simples
let idade = 20; // number (número) -> sem aspas
let altura = 1.80; // number (número) -> sem aspas
let masculino = true; // boolean (verdadeiro ou falso) -> sem aspas
let masculino = false; // boolean (verdadeiro ou falso) -> sem aspas

// const -> variável constante (recomendado) -> variável que não pode ser alterada 
const nome = "João"; // string (texto) -> aspas duplas ou simples
const idade = 20; // number (número) -> sem aspas
const altura = 1.80; // number (número) -> sem aspas
const masculino = true; // boolean (verdadeiro ou falso) -> sem aspas
const masculino = false; // boolean (verdadeiro ou falso) -> sem aspas

// Exemplo de variáveis e de declaraçoes de variáveis
var a,b,c; // declaração de variáveis
a = 10; // atribuição de valor
b = 20; // atribuição de valor
c = a + b; // atribuição de valor
console.log(c); // exibição do valor da variável c
*/

// Exemplo de variáveis e de declaraçoes de variáveis
var nome,sobrenome,nome_completo; // declaração de variáveis
nome = "Otavio"; // atribuição de valor
sobrenome = "Augusto"; // atribuição de valor
nome_completo = nome + " " + sobrenome; // atribuição de valor
console.log(nome_completo); // exibição do valor da variável nomecompleto
document.getElementById("texto").innerHTML = nome_completo // exibição do valor da variável nomecompleto no HTML (página web)





